import { Tag } from "src/core";

/**
 * Action tag for VDF output and proof submission
 */
export const POST_VDF_OUTPUT_AND_PROOF_TAG: Tag = {
    name: "Action",
    value: "Post-VDF-Output-And-Proof"
};

