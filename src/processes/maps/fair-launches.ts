import { PROCESS_IDS } from "../ids";
import { FairLaunchMap } from "./types";

export const GAME_FAIR_LAUNCH: FairLaunchMap = {
	name: "GAME",
	tokenProcessId: PROCESS_IDS.ARCAO.GAME_TOKEN,
	fairLaunchProcessId: PROCESS_IDS.AUTONOMOUS_FINANCE.FAIR_LAUNCH_PROCESSES.GAME
}
