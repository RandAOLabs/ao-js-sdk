// Cache Keys
export const CACHE_KEYS = {
    ANT: {
        GET_RECORDS: 'getRecords'
    }
} as const;

// Domain Name Separator
export const DOMAIN_SEPARATOR = '_';

// Record Types
export const ARN_ROOT_NAME = '@' as const;
